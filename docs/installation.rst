============
Installation
============

At the command line::

    $ easy_install django-admin-user-summary

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-user-summary
    $ pip install django-admin-user-summary
